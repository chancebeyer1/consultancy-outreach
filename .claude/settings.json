{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PROJECT_DIR:-$(git rev-parse --show-toplevel)}\" && { [ -n \"$(git status --porcelain)\" ] || exit 0; } && git add -A && git commit -q -m \"auto-checkpoint: $(date '+%Y-%m-%d %H:%M')\" && git push -q origin HEAD && { git push -q origin HEAD:main 2>/dev/null || true; }",
            "timeout": 90,
            "statusMessage": "Auto-committing & pushing…"
          }
        ]
      }
    ]
  }
}
