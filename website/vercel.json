{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "sh -c 'PREV=${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}; git cat-file -e \"$PREV^{commit}\" 2>/dev/null || exit 1; git diff --quiet \"$PREV\" \"${VERCEL_GIT_COMMIT_SHA:-HEAD}\" -- website'",
  "git": {
    "deploymentEnabled": {
      "feat/supabase-campaigns-unipile": false
    }
  }
}
