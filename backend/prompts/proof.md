# Proof / sales artifact

What the drafter can credibly claim about my recent work, and where to point readers. NDA-bound — never name the client, never describe what their product does, never reveal architecture specifics that aren't already public.

## The case study (NDA-compliant defaults)

> **TODO before going live:** decide one of three disclosure tiers. Tier 2 is the default — it's defensible under the NDA without further client sign-off.

### Tier 1 — fully generic (always safe)

> Just wrapped a multi-month contract building a production AI agent. Looking for the next one or two engagements like it.

### Tier 2 — describes my work, not their product (default)

> Spent the last several months as the agent engineer on a contract — built the orchestration layer, evals, and the production deploy for an LLM agent shipping to real users. Looking for the next one or two engagements like it.

### Tier 3 — names the client and product specifics (REQUIRES WRITTEN SIGN-OFF)

> Replace this block only if Allan Carroll at the client confirms in writing what
> you can say. Until then, do not use this tier.

## The link

`LANDING_URL` is in `.env`. Drop it (raw, not bracketed markdown) when the message naturally invites it. Never pitch the link — let it be a low-pressure offer.

## What I can claim (independent of any specific client)

- I build production-grade AI agents end-to-end (architecture → orchestration → evals → deploy → monitoring)
- I ship on startup timelines — weeks-to-production, not quarters
- Comfortable in Python + Next.js + Postgres + Anthropic SDK + standard LLM tooling
- Independent contractor — fast to ramp, no procurement overhead
- One shipped agent in production from a recent contract (details under NDA)

## What I cannot claim publicly

- The client name (NDA — generic descriptors only; even "Series A AI startup" combined with timing risks identification)
- What the agent does or who it serves
- Specific architecture choices that I learned were theirs (anything outside my pre-existing skillset)
- Funding stage / customer count / outcome metrics tied to them
- Stack details that aren't already public on their website
- Multi-client portfolio claims — I have one shipped case so far

## Soft-ask options (pick one per message)

1. Drop landing link, no ask: *"wrote up what I do here: {{landing_url}}"*
2. Offer to compare notes: *"happy to compare notes if you're shipping similar stuff"*
3. Offer a specific thing: *"happy to walk you through how I'd approach {{their specific problem}}"*
4. Plain interest: *"open to a chat if you're hiring contractors"*

Never combine more than one ask per message.
