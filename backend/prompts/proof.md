# Proof / sales artifact

This is what I can credibly claim and link to. The drafter should pull from here when it's natural — never force it.

## The case study (anonymized — confirm with StratEdge AI before going live)

> **TODO: replace with sign-off-approved copy. Draft version below.**
>
> 4-month engagement with a Series A AI startup. Built and shipped a production
> AI agent application that {{REPLACE: what the agent does in one sentence —
> e.g. "automates B2B sales research workflows" or "answers compliance questions
> against a 20k-doc knowledge base"}}.
>
> Stack: {{REPLACE: e.g. "Python, Anthropic Claude, LangGraph, Postgres, Modal,
> Next.js"}}.
>
> Outcome: {{REPLACE: one concrete metric — "cut customer onboarding from 3
> weeks to 4 days" or "agent handled 60% of tier-1 support volume" or
> "shipped v1 to production in 6 weeks"}}.

## The link

LANDING_URL is in `.env`. Drop it (raw, not bracketed markdown) when the message naturally invites it. Never pitch the link — let it be a low-pressure offer.

## What I can claim

- Built production-grade AI agents end-to-end (architecture → deploy → monitoring)
- Shipped on tight startup timelines (weeks, not quarters)
- Comfortable in Python + Next.js + Postgres + modern LLM tooling
- Independent contractor — fast to ramp, no long procurement
- 4-month proof point at a real Series A AI company

## What I cannot claim (until I have it)

- Multi-client portfolio (only have one shipped case so far)
- Specific client name (NDA — anonymized only)
- Vertical specialization beyond "AI agents" (haven't picked a niche)
- Open-source maintainer status (unless true — verify before writing)

## Soft-ask options (pick one per message)

1. Drop landing link, no ask: *"wrote up what I built here: {{landing_url}}"*
2. Offer to compare notes: *"happy to compare notes if you're shipping similar stuff"*
3. Offer a specific thing: *"happy to walk you through how I'd approach {{their specific problem}}"*
4. Plain interest: *"open to a chat if you're hiring contractors"*

Never combine more than one ask per message.
