# Proof / sales artifact

What the drafter can credibly claim about my recent work, and where to point readers. The drafter should pull from here when it's natural — never force it.

## The case study

> **TODO:** edit this to match what you actually shipped. Defaults below are a safe starting point.

> Recently wrapped a multi-month contract as the agent engineer on a production AI app — built the orchestration layer, evals, and the production deploy for an LLM agent shipping to real users. Looking for the next one or two engagements like it.

## The link

`LANDING_URL` is in `.env`. Drop it (raw, not bracketed markdown) when the message naturally invites it. Never pitch the link — let it be a low-pressure offer.

## What I can claim

- I build production-grade AI agents end-to-end (architecture → orchestration → evals → deploy → monitoring)
- I ship on startup timelines — weeks-to-production, not quarters
- Comfortable in Python + Next.js + Postgres + Anthropic SDK + standard LLM tooling
- Independent contractor — fast to ramp, no procurement overhead
- One shipped agent in production from a recent contract

## What I cannot yet claim

- Multi-client portfolio — only one shipped case so far
- Vertical specialization beyond "AI agents"
- Open-source maintainer status (unless true — verify before writing)

## Soft-ask options (pick one per message)

1. Drop landing link, no ask: *"wrote up what I do here: {{landing_url}}"*
2. Offer to compare notes: *"happy to compare notes if you're shipping similar stuff"*
3. Offer a specific thing: *"happy to walk you through how I'd approach {{their specific problem}}"*
4. Plain interest: *"open to a chat if you're hiring contractors"*

Never combine more than one ask per message.
